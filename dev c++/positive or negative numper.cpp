#include<stdio.h>
int main()
{
	int num;
	printf("enter the numper:");
	scanf("%d",&num);
	if(num>0)
	{
		printf("this is the positive numper");
		
	}
	else if(num==0)
	{		printf("the numper is 0");

		
	}
	else
	{
		printf("this is the negative numper");
	}
}
